# No ar

Peças que já foram publicadas. Uma pasta por peça, dentro da pasta da rede.

Chegam aqui de dois jeitos: publicação imediata (`publish.js` no LinkedIn,
`publish-graph-api.js` no Instagram) ou pelo `fila.js`, quando ele vê que o Worker
publicou o que estava agendado.

Cada pasta tem um `_estado.md` com o link do post, a data de publicação e de onde a
peça veio. É o histórico do que já saiu, então não apagar.
